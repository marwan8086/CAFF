# ============================================================
# CAFF -- Optional dependencies
# ============================================================
# Install only if you need the corresponding feature:
#
#   pip install -r requirements-optional.txt
#
# These are NOT required for training or evaluating CAFF on
# pre-extracted KGs and pre-annotated QA datasets.
# Biomedical NER (only needed if you run scripts/build_kg.py
# from raw text instead of pre-existing KG TSV files).
scispacy>=0.5,<0.6
spacy>=3.5,<3.8
# LLM backbone (only needed for end-to-end QA generation,
# paper section 8.3). Not used by the core CAFF model.
openai>=1.0,<2.0
# Experiment tracking (recommended but not required).
wandb>=0.15
